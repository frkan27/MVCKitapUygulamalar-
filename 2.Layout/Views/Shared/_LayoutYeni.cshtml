<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>
        @Html.ActionLink("AnaSayfa", "AnaSayfa", "Home")
        @Html.ActionLink("Hakkimizda", "Hakkimizda", "Home")
        @Html.ActionLink("İletişim", "Iletisim", "Home")

    </div>
    <div>
        @RenderBody()
    </div>
</body>
</html>
